{
  "name": "SquidHallBE",
  "version": "0.1.0",
  "description": "CoNZealand Squid Hall social space.",
  "main": "server/lib/index.js",
  "scripts": {
    "compile-server": "npm install --prefix server && cd server && npx tsc && cd -",
    "compile-client": "npm install --prefix client && cd client && npx webpack --env.production && cd -",
    "install": "npm run compile-server && npm run compile-client",
    "start": "DEBUG=colyseus:* node --optimize_for_size --max_old_space_size=940 --gc_interval=100 server/lib/index.js"
  },
  "engines": {
    "node": "12"
  },
  "repository": {
    "url": "https://github.com/ericgundrum/SquidHallBE",
    "type": "git"
  },
  "author": "",
  "license": "Apache 2.0"
}
